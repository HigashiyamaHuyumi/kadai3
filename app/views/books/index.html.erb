<p>---------------</p>

<%= render 'books/newbook' %>

<p>---------------</p>

<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
  </thead>
<tbody>

     <% @books.each do |book| %>
      <tr>
        <td><%= link_to book.title, book_path(book) %></td>
        <td><%= book.opinion %></td>
      </tr>
    <% end %>
  </tbody>
</table>